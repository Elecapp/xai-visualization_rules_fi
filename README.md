# xai-visualization_rules_fi

to install the package, run:
pip install XAI-Library

## Using the code with Jupyter Notebook
In some environemnts it is necessary to configure the Jupyter Notebook to use the correct environment. 
To do so, run the following command in the terminal:
```bash
python -m ipykernel install --user --name=[name of your environment]
```